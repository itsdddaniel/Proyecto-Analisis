<!DOCTYPE html>
<html>
    <head>
        <meta charset ="UTF-8">
        <meta name = "Author" content = "Daniel Arteaga">
        <meta name = "Clase" content = "Análisis y Diseño de Sistemas">
        <meta name = "Maestro" content = "Ingeniero Julio Sandoval">
        <meta name = "Periodo" content = "I PAC 2021">
        <link rel="stylesheet" type="text/css" href="../styles/indexStyle.css">
    </head>
    <body>
        <div class="wrapper">
            <header class="main-head">
                <div id="logoID">
                    <img src="images/Logo.png" alt="Proyecto Logo">
                </div>
            </header>
            <nav id="main-nav" class="main-nav">
            </nav>
            <table border="1">
                <tr>
                    <th>Producto</th>
                    <th>Precio</th>
                    <th>Fecha de Pago</th>
                    <th>Estado</th>
                </tr>
                <% try { %>
                    <%
                    if(data.length!=0){
                    var i=1;
                    data.forEach(function(test){
                    %>
                    <tr>
                        <td><%=test.producto %></td>
                        <td><%=test.productoPrecio %></td>
                        <td><%=test.fechaDePago %></td>
                        <td><%=test.estado %></td>
                    </tr>
                    <%  i++; }) %>
                    <% } else{ %>
                        <tr>
                            <td colspan="7">No has realizado ninguna compra.</td>
                        </tr>
                    <% } %>
                <% } catch { %>
                    <tr>
                        <td colspan="7">No has realizado ninguna compra.</td>
                    </tr>
                <% } %>
              </table>
                <aside id="side" class="side">
                    <button onclick="location.href='/back';">Regresar</button>
                </aside>
                <footer class="main-footer">Derechos Reservados 2021</footer>
        </div>
        <script src="../scripts/scriptTwo.js"></script>
    </body>
</html>